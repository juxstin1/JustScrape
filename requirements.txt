# JustScrape - Web Scraping & Search MCP Server

# Core scraping
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=4.9.1

# Async HTTP/2 scraping (Phase 4)
httpx[http2]>=0.27.0

# JavaScript rendering (optional, for JS-heavy sites)
playwright>=1.40.0

# MCP Server (Model Context Protocol)
mcp>=1.0.0

# Web search
duckduckgo-search>=6.0.0

# CLI interface
click>=8.1.0

# XML security
defusedxml>=0.7.1

# Data handling
python-dateutil>=2.8.0

# Content analysis (Phase 1 — snippet extraction)
trafilatura>=2.0.0
rank-bm25>=0.2.2
scikit-learn>=1.3.0

# Quality scoring and deduplication (Phase 2 — pipeline integration)
rapidfuzz>=3.0.0  # Near-duplicate snippet detection in deduplicate_results

# Optional enhancements
pyperclip>=1.8.0  # Clipboard support for CLI
# newspaper3k>=0.2.8  # News article extraction
# readability-lxml>=0.8.1  # Mozilla readability algorithm
